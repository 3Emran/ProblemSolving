#include <iostream>
using namespace std;
int main()
{
    string text="a quick brown fox jump over the lazy dog";
    //cout<<text.length();
    //we can also use size() function to find the length of a string
    cout<<text.size();
    return 0;
}
